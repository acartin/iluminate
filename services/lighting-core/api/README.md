# Lighting Core API

Reserved for API handlers for the LED choreography domain.

Expected future surface:

- projects and installations
- partituras
- assets metadata
- validation and export
- deployments
- desired/reported device state
- scene activation commands

API handlers should use `domain/`, `schemas/`, `validators/` and `storage/` instead of duplicating rules.
