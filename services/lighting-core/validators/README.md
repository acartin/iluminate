# Lighting Validators

Validation rules for partituras and deployment payloads.

Examples:

- missing references
- out-of-range segments
- overlapping LED ranges
- empty zones
- unsupported effects
- incompatible core versions

Physical LED limits, current estimates and brightness constants belong to the external firmware environment unless they are promoted into an explicit partitura compatibility contract later.
